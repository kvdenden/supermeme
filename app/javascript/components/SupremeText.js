import styled from "styled-components";

const SupremeText = styled.span`
  display: inline-block;
  font-family: futura-pt, sans-serif;
  font-weight: 600;
  font-style: italic;
  letter-spacing: -0.05em;
  transform: scaleY(1.1);
`;

export default SupremeText;
